import 'element_registry.dart';

/// Alias kept for reducer/context API readability.
///
/// The registry is now a single concrete implementation rather than an
/// abstract contract plus one default class.
typedef ElementRegistry = DefaultElementRegistry;
